# agustino-test-jenius
Jenius Technical Test

  - git clone first and go to folder repository
  - try to install dependencies 
  ```sh
  # In Terminal
  npm install
  ```
  - for start
  ```sh
  # In Terminal
  npm run start
  ```
